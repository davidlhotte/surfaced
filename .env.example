# Shopify
SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
SHOPIFY_SCOPES=read_content,write_content,read_themes,write_themes
SHOPIFY_APP_URL=http://localhost:3000

# Database (Vercel Postgres)
DATABASE_URL=

# Cache (Vercel KV)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Security
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=

# Payload CMS (Blog Admin)
# Generate with: openssl rand -hex 32
PAYLOAD_SECRET=

# Monitoring (Sentry - optional)
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development
LOG_LEVEL=info

# AI APIs (choose ONE option)

# Option 1: OpenRouter (RECOMMENDED - single API for all AI platforms)
# Get your key at: https://openrouter.ai/keys
# IMPORTANT: Never commit API keys to git! Always use environment variables.
# To run scripts: export OPENROUTER_API_KEY=your-key && npx tsx scripts/test-all-platforms.ts
OPENROUTER_API_KEY=

# Option 2: Direct API keys (fallback if OpenRouter not configured)
# OpenAI (for ChatGPT visibility checks and content optimization)
OPENAI_API_KEY=
# Perplexity (for Perplexity visibility checks)
PERPLEXITY_API_KEY=
# Google AI (for Gemini visibility checks)
GOOGLE_AI_API_KEY=
